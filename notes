1//0gWzDcPcFlv5oCgYIARAAGBASNwF-L9Ir8twGGqugQOpDemzg4pKAXR8vlkb6yts8p6MzezeKTp1_Negzty81olmkXf24nhs_S7Q


firebase deploy --token "$FIREBASE_TOKEN"

1//0g9tdwPtYfo2QCgYIARAAGBASNwF-L9Irx1ZnQL3WX5a0VNEzjG_WW0NFuGpY5c4fSzFL6vaWJA73YjAZMUHPxxCXpQBx_1Kzxtk


firebase deploy --token "$FIREBASE_TOKEN"


firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PA_DEVELOPMENT_PROJECT }}'

firebase-hosting-merge.yml 
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PA_DEVELOPMENT_PROJECT }}'
          channelId: live
          projectId: pa-development-project

firebase-hosting-pull-request.yml
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PA_DEVELOPMENT_PROJECT }}'
          projectId: pa-development-project


name: Build and Deploy
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Install Dependencies
        run: npm install
      - name: Build
        run: npm run Build
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          name: dist
          path: dist
  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Download Artifact
        uses: actions/download-artifact@master
        with:
          name: dist
          path: dist
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PA_DEVELOPMENT_PROJECT }}

1//0gzqg37p5BAlSCgYIARAAGBASNwF-L9IrOQk6lhJ92iPHBiRiatfN2RrAY0oymJPliiXOtNE3xvIs1RXe1VDGGXy6TA9Np2qBkAg 


firebase deploy --token "$FIREBASE_TOKEN"

